- [ ] Implement shared helper to compute age from `dob` when `age` is missing/0
- [ ] Update `backend/accounts/api_profiles.py` -> `add_profile_fields` to use computed age
- [ ] Update `backend/accounts/views.py` -> all endpoints that return card profiles (dashboard, pinned/interests, shortlist, blocked, profile detail, wishlist list)
- [ ] Manual verification: dashboard overview, matches popup, shortlisted, blocked, interests, profile modal

